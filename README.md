This script creates a list of employees with their working time information using security hole in an internal service of a company (let's not name it :).

The generated `list.html` file contains a list with links to each employee's working time table page.
